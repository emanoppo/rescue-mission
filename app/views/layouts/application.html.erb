<!DOCTYPE html>
<html>
<head>
  <title>RescueMission</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%if !current_user.nil? %>
    <h3>Signed in as: <%= current_user.name %></h3>
    <a href="/users/sign_out">Click to Sign Out</a>
  <% else %>
    <h3><a href="/users/sign_in">Click to Sign In</a></h3>
  <% end %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>

</body>
</html>
